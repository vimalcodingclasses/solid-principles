// Responsibility: Handling data persistence.
class EmployeeRepository {
    save(employee: EmployeeData): void {
        // Focused logic for database saving...
        console.log(`Saving ${employee.name} data to SQL database...`);
    }
}